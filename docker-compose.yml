services:
  onelab-api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: onelab-api
    restart: unless-stopped
    ports:
      - "8080:8080"
    env_file:
      - .env
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
